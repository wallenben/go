package main

import (
	"flag"
	"os"
	"fmt"
)

func main() {
	csvFilename := flag.String("csv", "problems.csv", "bruh")
	flag.Parse()
	file.err := os.Open()
	if err != nil{
		
	}else println("you'r file (%s) doesn't work", csvFilename)
	os.Exit(1)
}
